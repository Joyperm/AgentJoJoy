# Hook Enforcement Contract

Surface lifecycle:
- Optional Capability / Runtime Adapter:
- Loaded only for:
- Not loaded during:

Hook Contract Title:

Rule reference:

Optional hook label:

Purpose:

Owner intent:

Install scope:
- Personal-local:
- Workspace-local untracked:
- Repo-shared:
- CI/server-side:

When this hook should fire:

Runtime binding target:
- Claude Code:
- Codex:
- Antigravity:

Inputs the hook must inspect:

Decision behavior:
- allow:
- warn/advisory:
- ask:
- force_ask:
- deny/block:

Owner override policy:

Fallback when hook is unavailable:

What this hook must never do:

Validation checklist:
